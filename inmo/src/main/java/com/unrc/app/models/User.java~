package com.unrc.app.models;

import org.javalite.activejdbc.Model;

public class User extends Model {
  static{
      validatePresenceOf("web", "mail", "name", "id", "neighborhood", "street", "tel");
  }
}
